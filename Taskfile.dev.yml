version: "3"

vars:
  COMPOSE_LOCAL: "{{.ROOT_DIR}}/docker-compose.yml:{{.ROOT_DIR}}/compose/99_local.yml"

tasks:
  setup:
    desc: One-time local files (config.local.yml, config.secrets.local.yml) + .env
    dir: "{{.ROOT_DIR}}"
    cmds:
      - |
        set -euo pipefail
        if [[ ! -f config.local.yml ]]; then
          cp config.local.example.yml config.local.yml
          echo "created config.local.yml"
        else
          echo "config.local.yml already exists"
        fi
        if [[ ! -f config.secrets.local.yml ]]; then
          cp config.secrets.local.example.yml config.secrets.local.yml
          echo "created config.secrets.local.yml"
        else
          echo "config.secrets.local.yml already exists"
        fi
      - task: init
      - task: export-config

  init:
    desc: Ensure Docker (Colima profile docker) + data directories (run before compose/docker tasks)
    dir: "{{.ROOT_DIR}}"
    cmds:
      - bash ./scripts/ensure-docker.sh
      - task system:init

  colima:
    desc: Ensure Colima profile docker and context colima-docker (alias for docker part of init)
    dir: "{{.ROOT_DIR}}"
    cmds:
      - bash ./scripts/ensure-docker.sh

  docker-context:
    desc: Show active Docker context
    cmds:
      - docker context show

  export-config:
    desc: Merge config.yml + config.local.yml + local secrets into .env
    dir: "{{.ROOT_DIR}}"
    env:
      SBS_EXPORT_LOCAL: "1"
    cmds:
      - ./scripts/export_config.sh --local

  secrets-export:
    desc: Regenerate .env with local files (ignores SOPS unless you run system:secrets-export)
    cmds:
      - task: export-config

  enc-secrets:
    desc: Encrypt config.secrets.yml to config.secrets.enc.yml (SOPS; uses .sops.yaml)
    dir: "{{.ROOT_DIR}}"
    cmds:
      - |
        set -euo pipefail
        if [[ ! -f config.secrets.yml ]]; then
          echo "enc-secrets: missing config.secrets.yml (gitignored). Copy from config.secrets.example.yml" >&2
          exit 1
        fi
        if ! command -v sops >/dev/null 2>&1; then
          echo "enc-secrets: sops not found" >&2
          exit 1
        fi
        sops --encrypt --input-type yaml config.secrets.yml > config.secrets.enc.yml
        echo "enc-secrets: wrote config.secrets.enc.yml — review and git add/commit"

  demos-build:
    desc: Build all auth demo images
    dir: "{{.ROOT_DIR}}"
    deps: [init]
    env:
      COMPOSE_FILE: "{{.COMPOSE_LOCAL}}"
    cmds:
      - task: export-config
      - docker compose build auth-none auth-forward auth-forward-ops auth-oidc auth-jwt-api

  compose-config:
    desc: Validate prod + local compose merge
    dir: "{{.ROOT_DIR}}"
    deps: [init]
    env:
      COMPOSE_FILE: "{{.COMPOSE_LOCAL}}"
      SBS_EXPORT_LOCAL: "1"
    cmds:
      - task: export-config
      - docker compose config --quiet

  start:
    desc: Local stack — Docker init, local .env, compose with 99_local.yml
    dir: "{{.ROOT_DIR}}"
    deps: [init]
    env:
      COMPOSE_FILE: "{{.COMPOSE_LOCAL}}"
    cmds:
      - bash ./scripts/fix-data-permissions.sh
      - task: export-config
      - docker compose up -d --build

  stop:
    desc: Stop local stack (prod + 99_local compose files)
    dir: "{{.ROOT_DIR}}"
    env:
      COMPOSE_FILE: "{{.COMPOSE_LOCAL}}"
    cmds:
      - docker compose down
