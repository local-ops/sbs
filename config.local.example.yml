# Local overrides (gitignored). Copy: cp config.local.example.yml config.local.yml
# Merged after config.yml; only used by task dev:export-config / dev:start.
proxy:
  acme_email: dev@localhost

auth:
  authentik:
    domain: authentik.localhost

apps:
  n8n:
    domain: n8n.localhost
  static:
    ai_consult:
      domain: ai.localhost
      local_port: 9081
    whyeven:
      domain: whyeven.localhost
      local_port: 9080
    auth_demos:
      none:
        domain: auth-none.localhost
      forward:
        domain: auth-forward.localhost
      forward_ops:
        domain: auth-forward-ops.localhost
      oidc:
        domain: auth-oidc.localhost
        provider_slug: auth-oidc-demo
      jwt_api:
        domain: auth-jwt-api.localhost
        provider_slug: auth-jwt-api-demo
